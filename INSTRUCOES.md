Passo 1- instalar docker e docker-composer

linux usar o comando
sudo docker-compose up --build

windows
sudo docker-compose up --build

ele vai instalar todas as dependências e logo em seguida inicializar o projeto